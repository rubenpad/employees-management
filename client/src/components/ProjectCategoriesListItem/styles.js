import styled from 'styled-components';
import { colors } from '../../styles/index';

export const Item = styled.li`
  padding: 8px;
  cursor: pointer;
  user-select: none;

  input {
    width: 16px;
    height: 16px;
    margin-right: 8px;
  }
`;
